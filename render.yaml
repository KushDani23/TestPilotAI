services:
  - type: web
    name: testpilot-backend
    runtime: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/testpilot-backend-1.0.0.jar
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://testpilotai.vercel.app
